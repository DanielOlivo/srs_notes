import type { FC } from "react";
import { HeaderElements } from "../HeaderElements";

export const SettingsHeaderElements: FC = () => {
    return <HeaderElements buttonType="sidebar" title="Settings" />
}