import type { FC } from "react";
import { HeaderElements } from "../../../common/components/HeaderElements";

export const DocumentsHeaderElements: FC = () => {
    return <HeaderElements buttonType="sidebar" title="SRS Notes" />
}