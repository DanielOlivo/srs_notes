import type { FC } from "react";
import { HeaderElements } from "../../../common/components/HeaderElements";

export const DocumentConfigHeaderElements: FC = () => {
    return (
        <HeaderElements 
            buttonType="back"
            title="Document config"
        />
    )
}