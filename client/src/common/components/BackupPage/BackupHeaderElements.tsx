import type { FC } from "react";
import { HeaderElements } from "../HeaderElements";

export const BackupHeaderElements: FC = () => {
    return <HeaderElements buttonType="sidebar" title="Backup" />
}