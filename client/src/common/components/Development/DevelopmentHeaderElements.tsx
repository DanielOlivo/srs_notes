import type { FC } from "react";
import { HeaderElements } from "../HeaderElements";

export const DevelopmentHeaderElements: FC = () => {
    return <HeaderElements buttonType="sidebar" title="Development" />
}